const Button: React.FC = ({ children }) => (
    <button className="mt-4 w-full rounded-md border-2 border-dark px-3 py-2 text-center font-header text-lg text-dark">
        {children}
    </button>
);

export default Button;
